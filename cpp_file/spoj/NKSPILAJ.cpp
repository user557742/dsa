#include <bits\stdc++.h>
using namespace std;
int n;
const int N = 5001;
int a[N],b[N];
struct line{
    double x,double y;
};
line mline(int u,int v,int g,int h){
    
}
bool cat(line u,line v){
    if(u.x==v.x) return 0;
    double nx=1.0*(v.y-u.y)/(u.x-v.x);
    double ny=1.0*(v.y*u.x-u.y*v.x)/(u.x-v.x);
    
}
int main(){
    return 0;
}
